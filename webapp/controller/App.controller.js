sap.ui.define([
	"sap/ui/core/mvc/Controller"
], function (Controller) {
	"use strict";

	return Controller.extend("brightstart.ips.ui5.controller.App", {
		/**
		 * Inicialização do controlador
		 */
		onInit: function () {
			// Este controlador é apenas um container
			// Toda a lógica está nos controladores específicos de cada view
		}
	});
});
